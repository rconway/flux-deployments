apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cheese
spec:
  chart:
    spec:
      chart: ./charts/cheese
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  values:
    baseHostname: 192.168.49.2.nip.io
  interval: 1m0s
